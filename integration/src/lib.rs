//! Deliberately empty.
//!
//! This package exists only to own `tests/` and the host dependencies they
//! need, so that those dependencies stay out of the component's build graph.
//! See `Cargo.toml` for why that separation matters.
