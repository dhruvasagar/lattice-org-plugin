package lattice:plugin-host@0.1.0;

/// The UI-contribution surface (design.md §9.4 `ui`): status/gutter
/// segments, popups, notifications, sprite sets. Guest→host emits data only
/// — never draw calls (§7, paramount #1). Populated in PH7.9.
interface ui {}
